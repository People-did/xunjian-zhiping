<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.training.mapper.TrainingReportMapper">
    
    <resultMap id="ReportDTOMap" type="com.training.dto.ReportDTO">
        <id column="id" property="id"/>
        <result column="student_id" property="studentId"/>
        <result column="student_name" property="studentName"/>
        <result column="class_name" property="className"/>
        <result column="course_id" property="courseId"/>
        <result column="course_name" property="courseName"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="file_path" property="filePath"/>
        <result column="file_name" property="fileName"/>
        <result column="file_type" property="fileType"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    
    <select id="selectReportPage" resultMap="ReportDTOMap">
        SELECT 
            r.id,
            r.student_id,
            u.real_name as student_name,
            c.class_name,
            r.course_id,
            co.course_name,
            r.title,
            r.content,
            r.file_path,
            r.file_name,
            r.file_type,
            r.status,
            r.create_time,
            r.update_time,
            e.id as evaluation_id,
            e.completeness_score,
            e.specification_score,
            e.knowledge_score,
            e.total_score,
            e.ai_evaluation,
            e.manual_evaluation,
            e.evaluate_time
        FROM training_report r
        LEFT JOIN sys_user u ON r.student_id = u.id
        LEFT JOIN sys_class c ON u.class_id = c.id
        LEFT JOIN course co ON r.course_id = co.id
        LEFT JOIN evaluation_record e ON r.id = e.report_id
        <where>
            <if test="studentId != null">
                AND r.student_id = #{studentId}
            </if>
            <if test="courseId != null">
                AND r.course_id = #{courseId}
            </if>
            <if test="classId != null">
                AND u.class_id = #{classId}
            </if>
            <if test="status != null">
                AND r.status = #{status}
            </if>
        </where>
        ORDER BY r.create_time DESC
    </select>
    
    <select id="selectScoreByClassId" resultType="map">
        SELECT 
            u.id as student_id,
            u.real_name as student_name,
            c.class_name,
            r.title as report_title,
            e.completeness_score,
            e.specification_score,
            e.knowledge_score,
            e.total_score,
            e.evaluate_time
        FROM evaluation_record e
        LEFT JOIN training_report r ON e.report_id = r.id
        LEFT JOIN sys_user u ON r.student_id = u.id
        LEFT JOIN sys_class c ON u.class_id = c.id
        WHERE u.class_id = #{classId}
        ORDER BY e.total_score DESC
    </select>
    
    <select id="selectScoreByCourseId" resultType="map">
        SELECT 
            u.id as student_id,
            u.real_name as student_name,
            co.course_name,
            r.title as report_title,
            e.completeness_score,
            e.specification_score,
            e.knowledge_score,
            e.total_score,
            e.evaluate_time
        FROM evaluation_record e
        LEFT JOIN training_report r ON e.report_id = r.id
        LEFT JOIN sys_user u ON r.student_id = u.id
        LEFT JOIN course co ON r.course_id = co.id
        WHERE r.course_id = #{courseId}
        ORDER BY e.total_score DESC
    </select>
    
</mapper>

